# URL of the Brushly backend server (see ../server).
# Simulator on the same machine: http://localhost:4000
# Physical device: use your computer's LAN IP, e.g. http://192.168.1.20:4000
# Leave unset to run the app against local mock data (no backend required).
EXPO_PUBLIC_API_URL=http://localhost:4000
